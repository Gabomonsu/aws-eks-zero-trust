# This file lists checkov/custom checks intentionally skipped and the reason why.
# Each exception is documented — nothing is skipped silently.
skipped:
  - check_id: CKV_AWS_44
    reason: "RDS backup retention - en esta arquitectura la DB real va en AWS RDS aparte (no se define aquí)"
  - check_id: CKV_AWS_45
    reason: "S3 versioning - el bucket de estado se gestiona por separado con la cuenta de deploy"
  - check_id: CKV_AWS_94
    reason: "ALB desplegado por ALB Ingress Controller (kubernetes), no por Terraform"

# Enable parsing of .tf files outside terraform/ (e.g. within k8s dirs use kustomize)
directory: terraform